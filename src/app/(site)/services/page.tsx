import type { Metadata } from "next";
import WhatISolve from "@/components/WhatISolve";

export const metadata: Metadata = {
  title: "Services | Usman Nasir",
};

export default function ServicesPage() {
  return <WhatISolve />;
}
