import type { Metadata } from "next";
import Experience from "@/components/Experience";

export const metadata: Metadata = {
  title: "Experience | Usman Nasir",
};

export default function ExperiencePage() {
  return <Experience />;
}
